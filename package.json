{
  "name": "site-casamento-kaua-debora-vercel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --check api/public.js && node --check api/admin.js && node --check api/upload.js && node --check lib/db.js && node --check public/assets/js/site.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.6.1"
  }
}
